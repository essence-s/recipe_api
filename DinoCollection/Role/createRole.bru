meta {
  name: createRole
  type: http
  seq: 3
}

post {
  url: {{BACKEND_URL}}/role
  body: json
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

body:json {
  {
    "name":"dino",
    "permission": []
  }
}
